/**
 * Constantes de marca consumidas desde JavaScript (config de Clerk), donde un
 * `var(--token)` de CSS no es computable. Excepción documentada a la regla de
 * "cero hex fuera de los archivos de tokens" — mantener en sintonía con
 * `--accent-fill` en styles/nocturne.css.
 */
export const BRAND_FILL = '#059669'
