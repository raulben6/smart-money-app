import { PageHeader } from '@/components/shell/PageHeader'
import { ComingSoonCard } from '@/components/shell/ComingSoonCard'

/** Placeholder — un task posterior lo reemplaza con el comparador real de estudiantes. */
export default function ComparadorPage() {
  return (
    <>
      <PageHeader title="Comparador de estudiantes" subtitle="Selecciona a quién comparar y sobre qué métricas" />
      <ComingSoonCard />
    </>
  )
}
