import { PageHeader } from '@/components/shell/PageHeader'
import { ComingSoonCard } from '@/components/shell/ComingSoonCard'

/** Placeholder — un task posterior lo reemplaza con el flujo real de invitaciones (Clerk). */
export default function InvitacionesPage() {
  return (
    <>
      <PageHeader title="Invitaciones" subtitle="Invita a nuevos estudiantes a la plataforma" />
      <ComingSoonCard />
    </>
  )
}
